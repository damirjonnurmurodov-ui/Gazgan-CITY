name: gazgan_city
description: Gazgan City official city services Flutter UI prototype.
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.9.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^8.1.0
  go_router: ^17.2.3
  lucide_icons: ^0.257.0
  supabase_flutter: ^2.10.3
  url_launcher: ^6.3.2

flutter:
  uses-material-design: true
dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
